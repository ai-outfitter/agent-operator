# Replace repository locations and revisions before applying this sample.
apiVersion: link.aioutfitter.com/v1alpha1
kind: Organization
metadata:
  name: example-org
spec:
  displayName: Example Organization
  # Generic repositories; the researcher composition treats "wiki" as its wiki.
  repositories:
    - name: wiki
      uri: ssh://git@example.com/example-org/wiki.git
      defaultBranch: main
  # The MVP resolves a single pinned catalog. The list shape is kept so more can
  # be added later without a schema change.
  agentCatalogs:
    # A standalone owner/.agents repository has its Dotagents payload at root.
    - name: organization-agents
      github: example-org/.agents
      revision: 0123456789abcdef0123456789abcdef01234567
  # Projects grouping is deferred for the single-owner-fleet MVP (see OPR-002).
