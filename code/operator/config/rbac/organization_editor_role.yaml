# This rule is not used by the project link-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the link.aioutfitter.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: link-operator
    app.kubernetes.io/managed-by: kustomize
  name: organization-editor-role
rules:
- apiGroups:
  - link.aioutfitter.com
  resources:
  - organizations
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - link.aioutfitter.com
  resources:
  - organizations/status
  verbs:
  - get
